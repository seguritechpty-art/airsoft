# Project-wide Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# ============================================================
# 🗺️ MAPAS: MapLibre + OpenFreeMap ($0, SIN cuenta, SIN API key)
# ------------------------------------------------------------
# Los tiles los sirve el endpoint público de OpenFreeMap
# (https://tiles.openfreemap.org/styles/liberty), que no requiere
# registro, API key ni tarjeta. No hay nada que configurar aqui.
# Atribución de OpenStreetMap incluida automáticamente.
# ============================================================

# 🚀 BACKEND
# ------------------------------------------------------------
# Debug (emulador: 10.0.2.2 → localhost de tu PC)
# API_BASE_URL=http://10.0.2.2:3000
# Para dispositivo fisico usa la IP de tu PC en la red local:
# API_BASE_URL=http://192.168.1.50:3000
# Produccion (servidor desplegado):
# API_BASE_URL_PROD=https://tu-servidor.up.railway.app
# ============================================================